PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE `t_machines_locations` (
  `machine_id` integer NOT NULL,
  `location_id` integer NOT NULL
);
INSERT INTO "t_machines_locations" 
       (machine_id, location_id) 
VALUES (1, 2),
       (2, 2),
       (6, 2),
       (11, 2),
       (20, 1),
       (10, 1);

COMMIT;
